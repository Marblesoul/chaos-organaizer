image: Ubuntu

environment:
  nodejs_version: "18"

install:
  - nvm use %nodejs_version% || nvm install %nodejs_version%
  - node --version
  - npm --version
  - npm ci

build_script:
  - npm run lint
  - npm run build:client

artifacts:
  - path: client/dist/**
    name: frontend-dist
